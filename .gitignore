# Environment / secrets — never commit real env values
.env
.env.local
.env.*.local

# Downloaded corpus cache (official Fedlex XML) — re-fetchable, large
data/
!apps/evals/data/

# Private project notes — keep out of the public repo for now
docs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node
node_modules/
.next/
out/
*.tsbuildinfo

# ML / experiment tracking
mlruns/
mlartifacts/
models/

# Evals run output (regenerable JSON result files)
apps/evals/results/

# Evals LLM-drafted questions — unverified until a human curates them into gold.jsonl
apps/evals/data/gold.draft*.jsonl

# Crash dumps (Git Bash / Cygwin)
*.stackdump

# Agent workflow scratch (progress ledgers)
.superpowers/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
