# ingestion

Python CLI that builds the corpus (spec §4): resolve Fedlex SPARQL → fetch Akoma Ntoso XML → parse articles (1 article = 1 chunk) → embed via Ollama → index into pgvector.

Not implemented yet — Milestone 1–3.
