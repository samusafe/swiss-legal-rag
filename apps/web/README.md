# web

Next.js (App Router) + HeroUI chat UI (spec §6): SSE streaming, citation chips linking to Fedlex ELI pages, language switcher, retrieved-articles panel. Talks only to the `retrieval` API, never to Postgres.

Not implemented yet — Milestone 5.
